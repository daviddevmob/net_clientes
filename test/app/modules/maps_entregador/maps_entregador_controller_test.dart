import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:net_cliente/app/modules/maps_entregador/maps_entregador_controller.dart';
import 'package:net_cliente/app/modules/maps_entregador/maps_entregador_module.dart';

void main() {
  initModule(MapsEntregadorModule());
  // MapsEntregadorController mapsentregador;
  //
  setUp(() {
    //     mapsentregador = MapsEntregadorModule.to.get<MapsEntregadorController>();
  });

  group('MapsEntregadorController Test', () {
    //   test("First Test", () {
    //     expect(mapsentregador, isInstanceOf<MapsEntregadorController>());
    //   });

    //   test("Set Value", () {
    //     expect(mapsentregador.value, equals(0));
    //     mapsentregador.increment();
    //     expect(mapsentregador.value, equals(1));
    //   });
  });
}
