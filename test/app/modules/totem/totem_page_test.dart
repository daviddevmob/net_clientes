import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('TotemPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TotemPage(title: 'Totem')));
    //  final titleFinder = find.text('Totem');
    //  expect(titleFinder, findsOneWidget);
  });
}
