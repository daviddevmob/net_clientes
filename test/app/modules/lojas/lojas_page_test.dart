import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:net_cliente/app/modules/lojas/lojas_page.dart';

void main() {
  testWidgets('LojasPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(LojasPage(title: 'Lojas')));
    //  final titleFinder = find.text('Lojas');
    //  expect(titleFinder, findsOneWidget);
  });
}
