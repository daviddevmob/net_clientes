import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:net_cliente/app/modules/restaurantes/rest_profile/rest_profile_page.dart';

void main() {
  testWidgets('RestProfilePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RestProfilePage(title: 'RestProfile')));
    //  final titleFinder = find.text('RestProfile');
    //  expect(titleFinder, findsOneWidget);
  });
}
