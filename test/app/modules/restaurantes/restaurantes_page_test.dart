import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('RestaurantesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RestaurantesPage(title: 'Restaurantes')));
    //  final titleFinder = find.text('Restaurantes');
    //  expect(titleFinder, findsOneWidget);
  });
}
