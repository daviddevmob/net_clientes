import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FavoritasLojasPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FavoritasLojasPage(title: 'FavoritasLojas')));
    //  final titleFinder = find.text('FavoritasLojas');
    //  expect(titleFinder, findsOneWidget);
  });
}
