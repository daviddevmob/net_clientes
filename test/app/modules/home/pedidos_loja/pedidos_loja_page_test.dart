import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PedidosLojaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PedidosLojaPage(title: 'PedidosLoja')));
    //  final titleFinder = find.text('PedidosLoja');
    //  expect(titleFinder, findsOneWidget);
  });
}
