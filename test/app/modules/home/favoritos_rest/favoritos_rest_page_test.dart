import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:net_cliente/app/modules/home/favoritos_rest/favoritos_rest_page.dart';

void main() {
  testWidgets('FavoritosRestPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FavoritosRestPage(title: 'FavoritosRest')));
    //  final titleFinder = find.text('FavoritosRest');
    //  expect(titleFinder, findsOneWidget);
  });
}
