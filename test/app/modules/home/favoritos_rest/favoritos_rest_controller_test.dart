import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:net_cliente/app/modules/home/favoritos_rest/favoritos_rest_controller.dart';
import 'package:net_cliente/app/modules/home/favoritos_rest/favoritos_rest_module.dart';

void main() {
  initModule(FavoritosRestModule());
  // FavoritosRestController favoritosrest;
  //
  setUp(() {
    //     favoritosrest = FavoritosRestModule.to.get<FavoritosRestController>();
  });

  group('FavoritosRestController Test', () {
    //   test("First Test", () {
    //     expect(favoritosrest, isInstanceOf<FavoritosRestController>());
    //   });

    //   test("Set Value", () {
    //     expect(favoritosrest.value, equals(0));
    //     favoritosrest.increment();
    //     expect(favoritosrest.value, equals(1));
    //   });
  });
}
