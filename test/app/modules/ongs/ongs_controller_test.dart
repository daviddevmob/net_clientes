import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:net_cliente/app/modules/ongs/ongs_module.dart';

void main() {
  initModule(OngsModule());
  // OngsController ongs;
  //
  setUp(() {
    //     ongs = OngsModule.to.get<OngsController>();
  });

  group('OngsController Test', () {
    //   test("First Test", () {
    //     expect(ongs, isInstanceOf<OngsController>());
    //   });

    //   test("Set Value", () {
    //     expect(ongs.value, equals(0));
    //     ongs.increment();
    //     expect(ongs.value, equals(1));
    //   });
  });
}
