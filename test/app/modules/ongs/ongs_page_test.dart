import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:net_cliente/app/modules/ongs/ongs_page.dart';

void main() {
  testWidgets('OngsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(OngsPage(title: 'Ongs')));
    //  final titleFinder = find.text('Ongs');
    //  expect(titleFinder, findsOneWidget);
  });
}
