import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:net_cliente/app/modules/ongs/ong_profile/ong_profile_page.dart';

void main() {
  testWidgets('OngProfilePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(OngProfilePage(title: 'OngProfile')));
    //  final titleFinder = find.text('OngProfile');
    //  expect(titleFinder, findsOneWidget);
  });
}
