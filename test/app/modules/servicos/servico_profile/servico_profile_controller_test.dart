import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:net_cliente/app/modules/servicos/servico_profile/servico_profile_module.dart';

void main() {
  initModule(ServicoProfileModule());
  // ServicoProfileController servicoprofile;
  //
  setUp(() {
    //     servicoprofile = ServicoProfileModule.to.get<ServicoProfileController>();
  });

  group('ServicoProfileController Test', () {
    //   test("First Test", () {
    //     expect(servicoprofile, isInstanceOf<ServicoProfileController>());
    //   });

    //   test("Set Value", () {
    //     expect(servicoprofile.value, equals(0));
    //     servicoprofile.increment();
    //     expect(servicoprofile.value, equals(1));
    //   });
  });
}
