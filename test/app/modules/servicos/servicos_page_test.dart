import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:net_cliente/app/modules/servicos/servicos_page.dart';

void main() {
  testWidgets('ServicosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ServicosPage(title: 'Servicos')));
    //  final titleFinder = find.text('Servicos');
    //  expect(titleFinder, findsOneWidget);
  });
}
